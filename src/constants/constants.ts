import { HeaderType } from '../types'

export const BASIC_HEADERS: HeaderType = {
  'Content-Type': 'application/json',
  Accept: 'application/json'
}

export const DB_NAME = 'CacheDatabase'
export const STORE_NAME = 'CacheStore'
